UITableViewCell with right button that detects touches. Similar to the one in the App Store

This is an example app that shows how to have a UITableViewCell with a right button with three states. Suggestions for improvements are very welcome/

Todo:

* Add animations
* Detect touch in the whole table view